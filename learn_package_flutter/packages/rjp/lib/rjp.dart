library rjp;

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;

  /// mengembalikan nilai status string
  String status(String status) => "$status - status";
}
